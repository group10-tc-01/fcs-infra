apiVersion: apps/v1
kind: Deployment
metadata:
  name: fcs-identity
  namespace: fcs-identity
  labels:
    app.kubernetes.io/name: fcs-identity
    app.kubernetes.io/component: api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fcs-identity
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fcs-identity
        app.kubernetes.io/component: api
    spec:
      containers:
        - name: fcs-identity
          image: ghcr.io/group10-tc-01/fcs-identity:main
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: fcs-identity-local-config
          env:
            - name: ConnectionStrings__SqlServer
              valueFrom:
                secretKeyRef:
                  name: fcs-identity-local-secret
                  key: sqlserver-connection-string
            - name: Keycloak__AdminPassword
              valueFrom:
                secretKeyRef:
                  name: keycloak-client-secret
                  key: admin-password
            - name: ManagerSeed__Password
              valueFrom:
                secretKeyRef:
                  name: fcs-identity-local-secret
                  key: manager-password
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 18
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20
            failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: fcs-identity-service
  namespace: fcs-identity
  labels:
    app.kubernetes.io/name: fcs-identity
    app.kubernetes.io/component: api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: fcs-identity
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30083
