receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch:
    timeout: 5s
    send_batch_size: 1024

connectors:
  datadog/connector:

exporters:
  otlp/tempo:
    endpoint: tempo:4317
    tls:
      insecure: true

  otlphttp/loki:
    endpoint: http://loki:3100/otlp

  prometheus:
    endpoint: 0.0.0.0:9464
    namespace: fcs
    const_labels:
      environment: local

  datadog:
    api:
      key: ${env:DD_API_KEY}
      site: ${env:DD_SITE}

  debug:
    verbosity: basic

service:
  telemetry:
    logs:
      level: info
    metrics:
      address: 0.0.0.0:8888
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp/tempo, datadog/connector, datadog, debug]
    metrics:
      receivers: [otlp, datadog/connector]
      processors: [batch]
      exporters: [prometheus, datadog]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/loki, datadog]
